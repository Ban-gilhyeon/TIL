## 1. 데이터 모델링
## 정의 
- 현실 세계의 데이터를 데이터 관점으로 업무 분석하는 기법
- 데이터베이스를 구축하기 위한 분석 및 설계의 과정

![[Pasted image 20251105134726.png]]
## 특징 
1. 추상화 : 복잡한 현실을 단순화 
2. 단순화 : 불필요한 요소 제거
3. 명확성 : 누구나 이해할 수 있도록 표현

## 유의점
1. 중복 
2. 비유연성
3. 비일관성 : 데이터와 데이터간 상호 연관 관계에 대한 명확한 정의를 해야 함


## 설계 3가지 단계
개념적 -> 논리적 -> 물리적 
#### 개념적 데이터 모델링 
: 일의 데이터 간의 관계를 구상하는 단계 (현실 세계 --> 데이터 변환을 어떻게 할까?)
- 추상화 수준이 가장 높음 
- 피터 첸 표기법 (출처 : https://impa.tistory.com)
	- ![[Pasted image 20251105122815.png]]
	- ![[Pasted image 20251105122853.png]]
- 
#### 논리적 데이터 모델링
각 개념을 구체화
- ERD - RDB 모델 사상 
- 상세 속성 정의
- 정규화 
==> 내가 아는 ERD 작성 
#### 물리적 데이터 모델링
데이터베이스 생성 
SQL 문으로 테이블 생성하고 등등

### ERD

#### Crow'Foot
![[Pasted image 20251105142423.png]]

## 2. 엔티티 

### 특징
- Unique Key가 있어야 함
- 2개 이상의 인스턴스
- 속성이 있어야 함
- 다른 엔티티와 1개 이상의 관계를 갖고 있어야함

### 분류

유/무형에 따른 분류
- 유형 엔티티 : 물리적 형태, 안정성, 지속성 (회원, 상품 등)
- 개념 엔티티 : 물리적 형태 없음, 개념적 (부서, 학과 등)
- 사건 엔티티 : 행위로 발생 (주문, 이벤트 응모 등)

발생 시점에 따른 분류
- 기본 엔티티 : 업무에 원래 존재하는 정보(상품, 회원, 사원, 부서 등)
- 중심 엔티티 : 기본 엔티티로 부터 파생, 행위 엔티티 생성(주문, 매출, 계약 등)
- 행위 엔티티 : 2개 이상의 엔티티로부터 파생, 데이터가 자주 변경되거나 증가가능 (주문 내역, 이벤트 응모 등)

## 3. 속성

### 특징
- 더 이상 쪼갤 수 없는 단위
- 속성은 한개의 값만 가져야 함 (Atomic 원자성)
- 하나의 엔티티는 두개 이상의 속성을 가짐

### 분류
특성에 따른 분류 
<U>데이터 정합성이 고려되야 함 (정확성, 일관성, 신뢰성 유지)</U>
- 기본 속성(Basic) : 업무 프로세스를 통해 바로 정의 가장 많은 비중 차지
- 설계 속성(Designed) : 업무에 존재하지는 않지만, 필요하다고 판단해서 도출해낸 속성 (ex : id)
- 파생 속성(Derived) : 다른 속성의 속성값에 대해 계산된 값 또는 가공된 값 (ex : 재고, 좋아요 수 등)

## 4. 관계
- 존재 관계 : `부모`- `자식` 처럼 존재 자체로 연관성이 있는 관계 (직원 - 부서, 학생 - 학과 등)
- 행위 관계 : 행위를 함으로써 연관성이 생기는 관계 (회원 - 주문, 학생 - 출석 등)

#### 관계 표기법 

## 정규화

- 목표 : 데이터 정합성, 데이터 일관성 유지 및 보장
- 엔티티를 작은 단위로 분리하는 과정 

### 제1 정규화 
하나의 속성에 하나의 값만 들어가야 함 
![[Pasted image 20251112030504.png]]

### 제2 정규화 (조금 이해가 안됨)
![[Pasted image 20251112030607.png]]
중복 데이터로 인해 학생 이름으로 Row를 구분할 수 없음
나이 또한 Row를 구분할 수 없음 
학생 이름과 수강과목 두 가지를 합친 \[학생 이름, 수강과목] 기본키로만 Row를 구분할 수 있음
문제점 : 나이는 학생 이름에 종속되어져 있기에 학생이름 하나만 알더라도 나이를 알 수 있음 
**기본키 중에 특정 컬럼만 종속된 컬럼이 존재할 경우 2 정규형에 위배** 
![[Pasted image 20251112030839.png]]